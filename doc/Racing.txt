
.факторы:
  .1. можно в консоли нарисовать символ в любой точке.
    .1.1. перерисовывает символ в заданной позиции.
    .1.2. можо перерисовывать любое количество символов после.
    
  .2. Console.ReadKey
    .2.1. есть енум для кнопок ConsoleKey
    
  .3. потоки:
    .запуск в фоне.
    .сон потока.
    .совмесный доступ к консоле.
    
  .4. задан набор фигур:
    .балка, таран, квадрат.
    
текущая реализация:
  .2 массива:
    .фигура - занимает всё поле и перерисовывает его.
    .машинка - массив положения машинки шириной в ширину поля.
    
  .генератор фигуры:
    .какая + где.
    
  .перемещение машинки.
  